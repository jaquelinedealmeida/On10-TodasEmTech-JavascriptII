const listagemDeFrutas = ["Uva", "Banana", "Manga", "Cajá", "Pinha", "Maçã", "Melão"];

for (let fruta = 0; fruta < listagemDeFrutas.length; fruta++) {
    console.log(listagemDeFrutas[fruta]);
}

/* sintaxe

for (let index =0; index.lenght (condição para que o for pare para que ele não seja infinito); index++ (condição final para que ele passe dentor do próximo array)) {
  console.log(constante[index]);
}